apiVersion: 1

providers:
  - name: shorturl-observability
    orgId: 1
    folder: ShortUrl
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
